apiVersion: v1
kind: Service
metadata:
  name: agent-platform-api
  namespace: agent-platform-control
  labels:
    app.kubernetes.io/name: agent-platform-api
spec:
  selector:
    app.kubernetes.io/name: agent-platform-api
  ports:
    - port: 8080
      targetPort: http
      name: http
