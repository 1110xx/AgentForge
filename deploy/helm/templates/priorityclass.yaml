apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: agent-platform-control-critical
value: 1000
globalDefault: false
preemptionPolicy: PreemptLowerPriority
description: Control-plane API and reconciliation paths.
---
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: agent-platform-attempt
value: 1000
globalDefault: false
preemptionPolicy: Never
description: Disposable Attempt workloads; never preempt control-plane Pods.
